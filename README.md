## Reproducing the Results

#### To reproduce all results in the report:
Install dependencies: `sudo pip3 install matplotlib numpy termcolor`

Set desired queue sizes in `scripts/run.sh`:
- For example: `QSIZES=(20 100)`

Run the full experiment: `sudo scripts/run.sh`
